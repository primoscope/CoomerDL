# CoomerDL Web Application Configuration

# Application Settings
APP_NAME=CoomerDL
DEBUG=false
PORT=8080

# Database Configuration
# For local development, use SQLite:
DATABASE_URL=sqlite:///./downloads.db
# For production with PostgreSQL/Cloud SQL:
# DATABASE_URL=postgresql://user:password@localhost:5432/coomerdl

# Storage Configuration
# Options: "local" or "gcs"
STORAGE_TYPE=local
# For local storage:
DOWNLOAD_FOLDER=./downloads
# For Google Cloud Storage:
# GCS_BUCKET=your-bucket-name
# GOOGLE_CLOUD_PROJECT=your-project-id

# Security
# IMPORTANT: Change this in production!
SECRET_KEY=change-me-in-production-use-a-strong-random-key

# Redis (optional, for production)
# REDIS_URL=redis://localhost:6379/0

# CORS Origins (comma-separated)
# In production, set to your frontend domain
CORS_ORIGINS=*
